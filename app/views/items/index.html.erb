<table>
  <col width="200">
  <tr>
    <th>Title</th>
    <th></th>
  </tr>
<% @items.each do |item| %>
  <tr>
    <td><%= item.title %></td>
    <td><%= link_to 'Show', item_path(item) %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to 'Add item', new_item_path %>
